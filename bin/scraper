#!/usr/bin/env node

// Scraping backend
const Window = require('./classes/Window.js');

let window = new Window();

const scraper = async () => {
    window.init(() => {
        window.getFighters();
        //window.close();
    });
}

scraper();